/**
 * @type {import('express').RequestHandler}
 * get reactions for a specific post/comment
* For example: GET /reactions?postId=1234
               GET /reactions?postId=123&userId=123
 */
export default async (req, res) => { }
