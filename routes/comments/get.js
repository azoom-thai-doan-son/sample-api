/**
 * @type {import('express').RequestHandler}
 * get comments by post or user.
 * For example: GET /comments?postId=1&userId=2
 *              GET /comments?postId=1
 */
export default async (req, res) => { }
